1. when we have diffrent ways of doing the same behaviour we use stratogy.
2. when we want to select or change the behaviour type during run time.
3. to avoid the duplication of common behaviour shared accross some classes.(use interface implementation classes)
